import 'package:flutter/material.dart';

class URSScreenBody2 extends StatelessWidget {
  const URSScreenBody2({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Card(
        color: Colors.grey[900],
      ),
    );
  }
}
